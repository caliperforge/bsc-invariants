# Foundry
out/
cache/
broadcast/

# Echidna / Medusa (M2)
echidna-corpus/
medusa-corpus/
crytic-export/
.campaign-out/

# Halmos (deferred)
halmos-cache/

# Forge deps (managed by `forge install`; tags pinned in CI's foundry-setup action)
lib/forge-std/
lib/chimera/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env
.env
.env.local

# Receipts — override the repo-root `*.log` ignore for this experiment only.
# Receipts under receipts/ are dispatch deliverables (forge build/test proofs
# + planted-twin clean/planted leg evidence), not transient build noise.
!receipts/
!receipts/**/*.log
